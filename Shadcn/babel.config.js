module.exports = {
    presets: [
        '@babel/preset-env',        // For ECMAScript features
        '@babel/preset-react',      // For React JSX
        '@babel/preset-flow'        // For Flow syntax
    ],
    plugins: [
        // You can add any additional plugins here if necessary
    ]
};
